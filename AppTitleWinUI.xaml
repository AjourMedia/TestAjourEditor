<?xml version="1.0" encoding="utf-8" ?>
<Window 
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TestAjourEditor.AppTitleWinUI"
	>
	<Window.TitleBar>
		<TitleBar HeightRequest="32" BackgroundColor="{StaticResource editor_background}">
			<TitleBar.LeadingContent>
				<HorizontalStackLayout VerticalOptions="Center">
					<Image Source="ajour_logo.png" WidthRequest="24" HeightRequest="24" Margin="8,0,0,0" />
					<Label Text="Tu redacción" TextColor="{x:StaticResource editor_forground}" FontSize="16" Margin="8,0,0,0" VerticalTextAlignment="Center" />
				</HorizontalStackLayout>
			</TitleBar.LeadingContent>
			<TitleBar.TrailingContent>
				<HorizontalStackLayout VerticalOptions="Center">
					<Label Text="Ajour Media AS" FontSize="14" TextColor="Gray" Padding="0,0,0,0" />
				</HorizontalStackLayout>
			</TitleBar.TrailingContent>
		</TitleBar>
	</Window.TitleBar>
</Window>